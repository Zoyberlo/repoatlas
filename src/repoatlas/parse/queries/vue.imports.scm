; Vue's imports live in its script block, which is parsed as TypeScript and
; brings its own import query. This file exists so the language registry
; finds one, and matches nothing on the template itself.
(document) @_document
